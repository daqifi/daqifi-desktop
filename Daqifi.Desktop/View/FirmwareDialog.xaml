<Window x:Class="DAQifi.Desktop.View.FirmwareDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:dialogService="clr-namespace:Daqifi.Desktop.DialogService"
        mc:Ignorable="d"
        dialogService:DialogService.IsRegisteredView="True"
        Title="Firmware Settings" Height="300" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <GroupBox Header="Firmware Settings">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Label Grid.Column="0" Content="Firmware Version:"/>
                <Label Grid.Column="1" Content="{Binding Version}"/>
            </Grid>
        </GroupBox>
    </Grid>
</Window>
