<Controls:Flyout x:Class="Daqifi.Desktop.View.Flyouts.PerformanceMonitorFlyout"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
             xmlns:local="clr-namespace:Daqifi.Desktop.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="600"
             Width="{Binding FlyoutWidth}" Height="{Binding FlyoutHeight}" IsOpen="{Binding IsPerformanceMonitorOpen, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" Position="Right"
             Header="Performance Monitor">
    <Controls:Flyout.Resources>
        <local:OverallStatusToBrushConverter x:Key="OverallStatusToBrushConverter" />
        <local:MonitoringButtonContentConverter x:Key="MonitoringButtonContentConverter" />
        <!-- BooleanToVisibilityConverter is already in App.xaml as BoolToVis -->
    </Controls:Flyout.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Description -->
        <TextBlock Grid.Row="0" Text="Monitor data acquisition performance" FontSize="16" Margin="10,0,0,10"/>

        <!-- Status Indicator and Message -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,10">
            <Ellipse Width="32" Height="32" Margin="10,0,10,0">
                <Ellipse.Fill>
                    <Binding Path="PerformanceMonitorViewModel.OverallSystemStatus" Converter="{StaticResource OverallStatusToBrushConverter}"/>
                </Ellipse.Fill>
            </Ellipse>
            <TextBlock Text="{Binding PerformanceMonitorViewModel.OverallSystemStatusMessage}" FontSize="20" FontWeight="Bold" VerticalAlignment="Center"/>
        </StackPanel>

        <!-- Key Metrics -->
        <StackPanel Grid.Row="2" Orientation="Vertical" Margin="10,0,10,10">
            <!-- Sample Rate -->
            <StackPanel Orientation="Horizontal" Margin="0,5">
                <TextBlock Text="Sample Rate: " FontWeight="Bold"/>
                <TextBlock Text="{Binding PerformanceMonitorViewModel.ActualSampleRate, StringFormat={}{0:F0}/sec}"/>
                <TextBlock Text=" [target: " Foreground="Gray"/>
                <TextBlock Text="{Binding PerformanceMonitorViewModel.TargetSampleRate, StringFormat={}{0:F0}}" Foreground="Gray"/>
                <TextBlock Text="]" Foreground="Gray"/>
            </StackPanel>
            <!-- Avg. App Lag -->
            <StackPanel Orientation="Horizontal" Margin="0,5">
                <TextBlock Text="Avg. App Lag: " FontWeight="Bold"/>
                <TextBlock Text="{Binding PerformanceMonitorViewModel.AverageAppProcessingLagMs, StringFormat={}{0:F2} ms}"/>
            </StackPanel>
            <!-- Sampling Efficiency -->
            <StackPanel Orientation="Horizontal" Margin="0,5">
                <TextBlock Text="Sampling Efficiency: " FontWeight="Bold"/>
                <TextBlock Text="{Binding PerformanceMonitorViewModel.SamplingEfficiencyPercentage, StringFormat={}{0:F0}%}"/>
                <ProgressBar Width="120" Height="16" Margin="10,0,0,0" Value="{Binding PerformanceMonitorViewModel.SamplingEfficiencyPercentage}" Maximum="100">
                    <!-- TODO: Color code bar by PerformanceMonitorViewModel.SamplingEfficiencyStatus (Style/Trigger or converter) -->
                </ProgressBar>
            </StackPanel>
        </StackPanel>

        <!-- Controls -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Left" Margin="10,0,0,10">
            <Button Width="160" Height="36" Margin="0,0,10,0"
                    Command="{Binding PerformanceMonitorViewModel.ToggleEnabledCommand}">
                <Button.Content>
                    <TextBlock>
                        <Run Text="{Binding PerformanceMonitorViewModel.Enabled, Converter={StaticResource MonitoringButtonContentConverter}}"/>
                    </TextBlock>
                </Button.Content>
            </Button>
        </StackPanel>
    </Grid>
</Controls:Flyout>