<Controls:Flyout x:Class="Daqifi.Desktop.View.Flyouts.DevicesFlyout"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
             xmlns:vm="clr-namespace:Daqifi.Desktop.ViewModels"
             xmlns:networkDataModel="clr-namespace:Daqifi.Desktop.DataModel.Network;assembly=Daqifi.Desktop.DataModel"
             xmlns:sys="clr-namespace:System;assembly=mscorlib" 
             xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:local="clr-namespace:Daqifi.Desktop.Converters"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=vm:DaqifiViewModel}"
             Position="Right"
             Theme="Dark"
             IsPinned="True"
             Width="{Binding FlyoutWidth}"
             Height="{Binding FlyoutHeight}"
             Background="#1E1E1E"
             IsOpen="{Binding IsDeviceSettingsOpen, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}">
    
    <Controls:Flyout.Resources>
        <!-- Color Constants -->
        <Color x:Key="AccentColor">#4B8FE2</Color>
        <Color x:Key="BackgroundColor">#333333</Color>
        <Color x:Key="HeaderBackgroundColor">#2D2D30</Color>
        
        <!-- Brushes -->
        <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentColor}"/>
        <SolidColorBrush x:Key="BackgroundBrush" Color="{StaticResource BackgroundColor}"/>
        <SolidColorBrush x:Key="HeaderBackgroundBrush" Color="{StaticResource HeaderBackgroundColor}"/>
        
        <!-- Data Providers -->
        <ObjectDataProvider MethodName="GetValues" ObjectType="{x:Type sys:Enum}" x:Key="WifiModes">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="networkDataModel:WifiMode" />
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
        <ObjectDataProvider MethodName="GetValues" ObjectType="{x:Type sys:Enum}" x:Key="WifiSecurityTypes">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="networkDataModel:WifiSecurityType" />
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
        
        <!-- Converters -->
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <local:NotNullToBoolConverter x:Key="NotNullToBoolConverter"/>
        
        <!-- Text Styles -->
        <Style x:Key="HeaderTextBlockStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        
        <Style x:Key="SubHeaderTextBlockStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        
        <Style x:Key="SectionHeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="{StaticResource AccentBrush}"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>

        <Style x:Key="FormLabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="{StaticResource AccentBrush}"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>

        <!-- Control Templates -->
        <ControlTemplate x:Key="ComboBoxToggleTemplate" TargetType="ToggleButton">
            <Border Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition Width="30"/>
                    </Grid.ColumnDefinitions>
                    <ContentPresenter Grid.Column="0"
                                    Content="{Binding Path=SelectionBoxItem, RelativeSource={RelativeSource AncestorType={x:Type ComboBox}}}"
                                    ContentTemplate="{Binding Path=SelectionBoxItemTemplate, RelativeSource={RelativeSource AncestorType={x:Type ComboBox}}}"
                                    Margin="{TemplateBinding Padding}"
                                    VerticalAlignment="Center"/>
                    <Path Grid.Column="1" 
                          Data="M0,0 L8,8 L16,0" 
                          Stroke="White"
                          StrokeThickness="2"
                          VerticalAlignment="Center"
                          HorizontalAlignment="Center"/>
                </Grid>
            </Border>
            <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#404040"/>
                </Trigger>
                <Trigger Property="IsChecked" Value="True">
                    <Setter Property="Background" Value="#404040"/>
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>

        <!-- Control Styles -->
        <Style x:Key="TabControlStyle" TargetType="TabControl">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin" Value="0"/>
        </Style>

        <Style x:Key="TabItemStyle" TargetType="TabItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="#999999"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="MinWidth" Value="125"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border Name="Border" 
                                Background="Transparent"
                                BorderThickness="0,0,0,1" 
                                BorderBrush="{StaticResource BackgroundBrush}"
                                Height="40">
                            <Grid>
                                <Border Name="SelectedBorder"
                                        BorderThickness="0,0,0,2"
                                        BorderBrush="Transparent"
                                        VerticalAlignment="Bottom">
                                    <ContentPresenter x:Name="ContentSite"
                                                    VerticalAlignment="Center"
                                                    HorizontalAlignment="Center"
                                                    ContentSource="Header"
                                                    RecognizesAccessKey="True"/>
                                </Border>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="SelectedBorder" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                                <Setter Property="Foreground" Value="{StaticResource AccentBrush}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="False">
                                <Setter Property="Foreground" Value="#999999"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ButtonStyle" TargetType="Button" BasedOn="{StaticResource MahApps.Styles.Button}">
            <Setter Property="Background" Value="{StaticResource BackgroundBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="MinWidth" Value="80"/>
            <Setter Property="Padding" Value="15,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="2">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="AccentButtonStyle" TargetType="Button" BasedOn="{StaticResource ButtonStyle}">
            <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
        </Style>

        <!-- Input Control Styles -->
        <Style x:Key="TextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource BackgroundBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style x:Key="ComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Background" Value="{StaticResource BackgroundBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                        Padding="{TemplateBinding Padding}"
                                        Template="{StaticResource ComboBoxToggleTemplate}"/>
                            <Popup x:Name="PART_Popup"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True"
                                   Placement="Bottom"
                                   PopupAnimation="Slide"
                                   PlacementTarget="{Binding ElementName=ToggleButton}">
                                <Border Background="{StaticResource BackgroundBrush}"
                                        BorderThickness="0">
                                    <ScrollViewer MaxHeight="200" 
                                                VerticalScrollBarVisibility="Auto">
                                        <StackPanel IsItemsHost="True"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Resources>
                <Style TargetType="ComboBoxItem">
                    <Setter Property="Background" Value="{StaticResource BackgroundBrush}"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="Height" Value="35"/>
                    <Setter Property="Padding" Value="10,0"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ComboBoxItem">
                                <Border x:Name="Border"
                                        Background="{TemplateBinding Background}"
                                        Padding="{TemplateBinding Padding}">
                                    <ContentPresenter VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#404040"/>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Style.Resources>
        </Style>
    </Controls:Flyout.Resources>

    <DockPanel LastChildFill="True">
        <!-- Header -->
        <Grid DockPanel.Dock="Top" Background="#2D2D30" Height="65">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <!-- Device Info -->
            <StackPanel Margin="0,8,20,8">
                <TextBlock Text="{Binding SelectedDevice.DevicePartNumber}"
                         Style="{StaticResource HeaderTextBlockStyle}"/>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="S/N: " Style="{StaticResource SubHeaderTextBlockStyle}"/>
                    <TextBlock Text="{Binding SelectedDevice.DeviceSerialNo}" Style="{StaticResource SubHeaderTextBlockStyle}"/>
                    <TextBlock Text=" | v" Style="{StaticResource SubHeaderTextBlockStyle}" Margin="10,0"/>
                    <TextBlock Text="{Binding SelectedDevice.DeviceVersion}" Style="{StaticResource SubHeaderTextBlockStyle}"/>
                </StackPanel>
            </StackPanel>
        </Grid>

        <!-- Tab Navigation -->
        <TabControl Style="{StaticResource TabControlStyle}">
            <TabControl.Items>
                <TabItem Header="DATA ACQUISITION" Style="{StaticResource TabItemStyle}">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <StackPanel Margin="25">
                            <TextBlock Text="SAMPLING CONFIGURATION" Style="{StaticResource SectionHeaderStyle}"/>
                            <TextBlock Text="Frequency (Hz)" Foreground="White" Margin="0,0,0,10"/>
                            <TextBox Text="{Binding ElementName=FrequencySlider, Path=Value, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                                    Style="{StaticResource TextBoxStyle}"
                                    HorizontalAlignment="Left"
                                    Width="100"
                                    FontSize="24"
                                    TextAlignment="Center"
                                    Margin="0,0,0,15"/>
                            <Slider Name="FrequencySlider"
                                   Minimum="1" Maximum="1000"
                                   TickFrequency="1"
                                   IsSnapToTickEnabled="True"
                                   Value="{Binding SelectedDevice.StreamingFrequency, Delay=500, UpdateSourceTrigger=PropertyChanged}"/>
                        </StackPanel>
                    </ScrollViewer>
                </TabItem>

                <TabItem Header="SD CARD" Style="{StaticResource TabItemStyle}">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <StackPanel Margin="25">
                            <TextBlock Text="LOGGING STATE" Style="{StaticResource SectionHeaderStyle}"/>
                            <Controls:ToggleSwitch 
                                IsOn="{Binding IsSdCardLoggingEnabled, Mode=TwoWay}"
                                OnContent="Enabled" 
                                OffContent="Disabled"
                                Margin="0,0,0,25"/>

                            <TextBlock Text="DATA FORMAT" Style="{StaticResource SectionHeaderStyle}"/>
                            <ComboBox Style="{StaticResource ComboBoxStyle}"
                                    Margin="0,0,0,25"
                                    SelectedItem="{Binding SelectedDataFormat, Mode=TwoWay}">
                                <ComboBoxItem Content="Protobuf"/>
                                <ComboBoxItem Content="JSON"/>
                            </ComboBox>

                            <TextBlock Text="STORED FILES" Style="{StaticResource SectionHeaderStyle}"/>
                            <DockPanel LastChildFill="False" Margin="0,0,0,10">
                                <Button Style="{StaticResource ButtonStyle}"
                                        Content="refresh list"
                                        DockPanel.Dock="Left"
                                        Command="{Binding RefreshSdCardFilesCommand}"/>
                                <Button Style="{StaticResource AccentButtonStyle}"
                                        Content="download selected"
                                        DockPanel.Dock="Right"
                                        Command="{Binding DownloadSdCardFileCommand}"
                                        IsEnabled="{Binding SelectedSdCardFile, Converter={StaticResource NotNullToBoolConverter}}"/>
                            </DockPanel>

                            <ListView Background="#333333"
                                    BorderThickness="0"
                                    Foreground="White"
                                    Height="250"
                                    ItemsSource="{Binding SdCardFiles}"
                                    SelectedItem="{Binding SelectedSdCardFile, Mode=TwoWay}">
                                <ListView.View>
                                    <GridView>
                                        <GridViewColumn Header="FILE NAME" Width="250" DisplayMemberBinding="{Binding FileName}"/>
                                        <GridViewColumn Header="SIZE" Width="100" DisplayMemberBinding="{Binding FormattedSize}"/>
                                    </GridView>
                                </ListView.View>
                            </ListView>
                        </StackPanel>
                    </ScrollViewer>
                </TabItem>

                <TabItem Header="NETWORK" Style="{StaticResource TabItemStyle}">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <StackPanel Margin="25">
                            <TextBlock Text="WIFI MODE" Style="{StaticResource FormLabelStyle}"/>
                            <ComboBox Style="{StaticResource ComboBoxStyle}"
                                    Margin="0,0,0,20"
                                    ItemsSource="{Binding Source={StaticResource WifiModes}}"
                                    SelectedItem="{Binding SelectedDevice.NetworkConfiguration.Mode}">
                                <ComboBox.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Converter={StaticResource EnumDescription}}"/>
                                    </DataTemplate>
                                </ComboBox.ItemTemplate>
                            </ComboBox>

                            <TextBlock Text="SECURITY TYPE" Style="{StaticResource FormLabelStyle}"/>
                            <ComboBox Style="{StaticResource ComboBoxStyle}"
                                    Margin="0,0,0,20"
                                    ItemsSource="{Binding Source={StaticResource WifiSecurityTypes}}"
                                    SelectedItem="{Binding SelectedDevice.NetworkConfiguration.SecurityType}">
                                <ComboBox.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Converter={StaticResource EnumDescription}}"/>
                                    </DataTemplate>
                                </ComboBox.ItemTemplate>
                            </ComboBox>

                            <TextBlock Text="NETWORK SSID" Style="{StaticResource FormLabelStyle}"/>
                            <TextBox Style="{StaticResource TextBoxStyle}"
                                    Margin="0,0,0,20"
                                    Text="{Binding SelectedDevice.NetworkConfiguration.Ssid, Mode=TwoWay}"/>

                            <TextBlock Text="PASSWORD" Style="{StaticResource FormLabelStyle}"/>
                            <TextBox Style="{StaticResource TextBoxStyle}"
                                    Margin="0,0,0,25"
                                    Text="{Binding SelectedDevice.NetworkConfiguration.Password, Mode=TwoWay}"/>

                            <Button Content="APPLY NETWORK SETTINGS"
                                    Style="{StaticResource AccentButtonStyle}"
                                    HorizontalAlignment="Right"
                                    Command="{Binding UpdateNetworkConfigurationCommand}"/>
                        </StackPanel>
                    </ScrollViewer>
                </TabItem>

                <TabItem Header="FIRMWARE" Style="{StaticResource TabItemStyle}">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <StackPanel Margin="25">
                            <TextBlock Text="UPDATE DEVICE FIRMWARE" Style="{StaticResource SectionHeaderStyle}"/>
                            
                            <DockPanel LastChildFill="True" Margin="0,0,0,15">
                                <Button DockPanel.Dock="Right"
                                        Content="browse"
                                        Style="{StaticResource ButtonStyle}"
                                        Margin="10,0,0,0"
                                        Command="{Binding BrowseForFirmwareCommand}"/>
                                <TextBox Style="{StaticResource TextBoxStyle}"
                                        Text="{Binding FirmwareFilePath, Mode=TwoWay}"/>
                            </DockPanel>

                            <DockPanel LastChildFill="True">
                                <Button DockPanel.Dock="Right"
                                        Content="update"
                                        Style="{StaticResource AccentButtonStyle}"
                                        Margin="10,0,0,0"
                                        Width="80"
                                        Command="{Binding UploadFirmwareCommand}"/>
                                <controls:MetroProgressBar Height="35"
                                                         Background="#333333"
                                                         Foreground="#4B8FE2"
                                                         Minimum="0"
                                                         Maximum="100"
                                                         Value="{Binding UploadFirmwareProgress}"/>
                            </DockPanel>
                        </StackPanel>
                    </ScrollViewer>
                </TabItem>
            </TabControl.Items>
        </TabControl>
    </DockPanel>
</Controls:Flyout>
